<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin"> 

   <select id="selectAll" resultType="UserVO">
   		select uemail, uname, ucheck from userList
   </select>
   
   <select id="select" resultType="UserVO">
   		select uemail, uname, upass from userList where uname = #{uname}
   </select>
   
   <delete id="delete">
   		delete from userList where uname=#{uname}
   </delete>
   
   
   
   
   <!-- 일반회원(상세폼에 입력된)글 수정 -->
   <update id="update">
   		update userList
   		set uemail=#{uemail}, uname=#{uname}, ucheck=#{ucheck}
   		where uemail = #{uemail}
   </update>

   <select id="listAll" resultType="UserVO">
   		select uemail, uname, ucheck
   		from userList
   		order by uname
   </select>
</mapper>